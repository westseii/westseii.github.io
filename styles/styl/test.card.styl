class--test-card()
  .test-card
    padding: 20px;
    border-radius: 14px;
    background: weiWhite;
    mixin--box-shadow(2px, 0.1);

    &:nth-child(n+2)
      margin-top: 20px;

  .para
    text-indent: 20px;
